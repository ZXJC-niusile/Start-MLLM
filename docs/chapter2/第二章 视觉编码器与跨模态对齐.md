# 第二章 视觉编码器与跨模态对齐

## 一、为什么图像不能直接输入 LLM

语言模型习惯处理的是离散 token 序列，例如：

```text
["这", "是", "一", "张", "猫", "的", "图", "片"]
```

而图像本质上是一个像素张量，例如 `H x W x C`。它既不是离散 token，也没有天然的线性顺序。要想让 LLM 读懂图像，第一步必须先把图像编码成一组可计算的视觉 token。

这就是视觉编码器的职责。

## 二、视觉编码器在系统里的角色

视觉编码器的核心任务是：

1. 从像素中提取语义特征。
2. 保留必要的空间关系和局部细节。
3. 输出一个后续模块能使用的特征序列。

对多模态系统来说，它相当于“眼睛”。如果这双眼睛看得不清，后面的语言模型再强，也只是在对模糊信号做生成。

## 三、从 CNN 到 ViT

早期视觉模型更多依赖 CNN，优势是局部归纳偏置强，适合做分类、检测、分割。但在大模型时代，Transformer 风格的视觉编码器更容易与 LLM 体系接轨。

Vision Transformer（ViT）的关键思路很简单：

1. 把图像切成很多固定大小的 patch。
2. 把每个 patch 展平成向量。
3. 像处理文本 token 一样处理这些 patch token。

例如，一张 `448 x 448` 的图片，如果 patch size 是 `14 x 14`，那大致会得到 `32 x 32 = 1024` 个 patch token。分辨率越高，token 越多，感知越细，但计算成本也越高。

## 四、Patch Token 为什么重要

多模态模型经常在以下能力上拉不开差距：

- 能不能看清小字
- 能不能看懂复杂版面
- 能不能定位特定区域

根本原因之一，就是视觉 token 的粒度。粒度越粗，细节越容易丢；粒度越细，算力和显存越高。

因此在工程里你会经常看到这些折中：

- 限制输入分辨率
- 动态裁剪图像
- 只保留部分 patch token
- 对高分辨率图像先做 tile 切分再编码

## 五、跨模态对齐在解决什么问题

即使视觉编码器已经把图片变成了向量，这些向量也不一定和语言语义对得上。对齐的目标是让：

- 图像里的“猫”靠近文本里的“猫”
- 图像里的“红色按钮”靠近文本里的“红色按钮”
- 图像整体语义能够被语言模型利用

换句话说，对齐是在回答：**视觉特征怎样才能变成语言模型有意义的上下文。**

## 六、CLIP 是理解对齐最好的起点

CLIP 的核心训练方式是图文对比学习。它有一张图像编码器塔和一张文本编码器塔，训练目标是：

- 拉近正确图文对
- 拉远不匹配图文对

你可以把它想成“让图片和文字在同一个语义坐标系里站稳位置”。

### 一个直观理解

如果模型看到一张“黄色小狗在草地奔跑”的图片，那么它应该让这张图片的向量靠近：

- “一只黄色的小狗”
- “草地上的狗”
- “奔跑的小狗”

同时远离：

- “夜晚的城市街景”
- “一架飞机停在机场”

这种训练方式对检索、分类迁移、跨模态匹配非常有效，也为后来的生成式多模态模型打下了基础。

## 七、对齐之后还差什么

CLIP 解决的是“相似不相似”的问题，它擅长判别式匹配，但不擅长自由生成。你不能仅靠 CLIP 直接完成一个复杂的开放式视觉问答系统。

因此，后续生成式多模态模型通常还会加入：

- 一个 Connector 或 Projector，把视觉特征映射进语言空间
- 一个 LLM，负责把这些特征组织成答案
- 更贴近助手场景的指令数据

## 八、常见对齐方式

### 1. 全局对齐

把整张图片压成一个全局向量，再和文本向量对齐。适合检索、粗粒度匹配，但细节能力有限。

### 2. Token 级对齐

保留 patch token 或 region token，让模型学习更细粒度的图文关系。这对 OCR、定位、复杂推理更有帮助。

### 3. 通过 Connector 对齐到 LLM

视觉编码器输出的特征，经过线性层、MLP、Cross-Attention 或 Query 模块，转成 LLM 能吸收的输入表示。这是现代 VLM 的常见做法。

## 九、工程上如何判断“对齐没做好”

如果一个多模态模型出现下面这些现象，通常说明视觉信息没有真正进入推理链路：

- 对任何图片都回答模板化内容
- 回答主要依赖文字提示，而不是图像证据
- 图像换了，但回答几乎不变
- 图像中出现多个对象时，模型经常混淆主次
- 遇到细节问答时大面积幻觉

这时候不要急着只改 prompt，更应该回头检查：

- 输入分辨率和预处理是否合理
- 模型是否支持你当前的任务类型
- 服务端是否正确处理了图像输入
- 是否用了错误的 chat template 或 content schema

## 十、理解视觉编码器时的三个视角

建议你以后看任何多模态论文或仓库时，都用这三个问题去拆：

1. 输入图像如何切分与编码？
2. 输出的是全局向量、局部 token，还是多尺度表示？
3. 这些视觉特征如何与语言模型连接？

只要这三点清楚，模型结构就基本不会看乱。

## 十一、实战衔接：自己观察分辨率和细节丢失

你现在就可以做一个简单实验，不需要训练任何模型：

1. 准备一张包含小字、按钮、图标的截图。
2. 保存原图，再手动缩小成一个低分辨率版本。
3. 分别把两张图喂给同一个多模态模型，问同样的问题。
4. 记录两次回答里哪些细节丢失了。

这个实验会非常直观地告诉你：视觉编码器和输入分辨率对结果影响有多大。

进一步，你还可以观察：

- 自然图像和 UI 截图在感知难度上的差异
- “描述图片”与“回答细节问题”对视觉 token 的要求差异
- 长图、密集表格、代码截图为什么更难

## 十二、章末练习

1. 为什么图像不能直接像文本 token 一样喂给 LLM？
2. 用自己的话解释 `patch token` 和输入分辨率的关系。
3. CLIP 解决了什么问题，又没有解决什么问题？
4. 如果一个模型对任何图片都输出模板化答案，你会优先检查哪些地方？

## 十三、配图占位建议

- 建议图 1
  建议文件名：`docs/images/ch2-patch-tokenization.png`
  插入位置：第三节后
  画面描述：图片切成 patch token 并输入 ViT 的结构示意图。
- 建议图 2
  建议文件名：`docs/images/ch2-clip-alignment.png`
  插入位置：第六节后
  画面描述：CLIP 图像塔和文本塔通过对比学习完成语义对齐的图示。

## 十四、本章小结

- 视觉编码器负责把像素变成视觉 token。
- 对齐的目标，是把视觉信息送入一个语言可理解的语义空间。
- CLIP 是对齐路线的起点，但不是生成式视觉助手的终点。

下一章我们就沿着这条线，正式进入多模态生成架构。

## 十五、章节跳转

- 上一篇：[第一章 多模态大模型概览](../chapter1/第一章%20多模态大模型概览.md)
- 下一篇：[第三章 多模态生成架构](../chapter3/第三章%20多模态生成架构.md)
- 对应实践：[第七章 动手跑通你的第一个 VLM](../chapter7/第七章%20动手跑通你的第一个%20VLM.md)
